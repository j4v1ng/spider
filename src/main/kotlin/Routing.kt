package com.javing

import io.ktor.server.application.*
import io.ktor.server.routing.*

fun Application.configureRouting() {
    // Base routing configuration
    routing {
        // Routes are defined in SpiderController
    }
}
